#include<stdio.h>
#include<string.h>
int main(){
    char name[] = "padma";
    int length = strlen(name);
    printf("The length of this string is %d",length);
    printf("\n");
    return 0;
}