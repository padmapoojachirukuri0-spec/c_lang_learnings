#include<stdio.h>
int main()
    {
        int a;
        int b;
        printf("Enter 2 numbers:");
        scanf("%d %d",&a,&b);
        printf("sum of the numbers is: %d",a+b);
        return 0;

    }
